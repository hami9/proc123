{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    [
      "@semantic-release/release-notes-generator",
      {
        "$comment": "Every release page is someone's first contact with the project, and the commit list alone does not tell them how to install it. `writerOpts` is merged over the preset's, so only the footer changes. `{{version}}` interpolates, which keeps the asset filenames right without anyone editing this per release — the Tauri bundles are named by the version inside tauri.conf.json, so this stays correct only as long as sync-version.mjs keeps writing it. Two of the notes exist because they are the ways an install goes wrong and looks broken: about:addons rejecting the Firefox zip, and SmartScreen hiding the Run button on the unsigned MSI.",
        "writerOpts": {
          "footerPartial": "\n---\n\n## Installing\n\n**Desktop app — Windows** — download `proc123_{{version}}_x64_en-US.msi` and run it. Nothing else is needed: WebView2 ships with Windows 11 and with any current Windows 10.\n\n> The installer is **unsigned**, so Windows opens a SmartScreen panel with the Run button hidden behind **More info → Run anyway**. Signing needs a certificate issued to a named person and costs money, so until that decision is made the warning is expected and is not a sign anything is wrong.\n\n**Desktop app — Linux** — on Debian or Ubuntu, `sudo apt install ./proc123_{{version}}_amd64.deb`. On anything else take `proc123_{{version}}_amd64.AppImage`, `chmod +x` it and run it. The `.deb` is the small one because it links the system WebKitGTK; the AppImage carries its own, which is the whole size difference.\n\n**Chrome · Edge · Brave** — unzip `proc123-chrome-{{version}}.zip`, open `chrome://extensions`, turn on **Developer mode**, then **Load unpacked** and pick the unzipped folder.\n\n**Firefox** — open `about:debugging#/runtime/this-firefox`, click **Load Temporary Add-on…**, and select `proc123-firefox-{{version}}.zip` directly. No need to unzip.\n\n> Do **not** use `about:addons` → Install Add-on From File. That screen only accepts a Mozilla-signed file and rejects the zip as \"not verified\". Firefox has required signatures since version 48, and `xpinstall.signatures.required` is ignored on release builds, so no setting changes it. A temporary add-on lasts until you close the browser.\n\n**A terminal, no browser** — download `proc123-win32-x64.exe` or `proc123-linux-x64`, then `proc123 <category-url> -o products.csv`.\n\n### Which one do you want?\n\nThe **app** is the least work: it fetches any shop directly, renders JavaScript-built stores itself, and writes the CSV straight to disk. The **extension** is the only one that can read a shop you are already **logged in to**, because it runs inside your own browser session — nothing else can substitute for that. They are not alternatives, and installing both is the ordinary setup.\n\nproc123 is not in the Chrome Web Store or listed on addons.mozilla.org yet, which is why the extension install is a folder rather than a button — [publishing.md](https://github.com/hami9/proc123/blob/main/docs/publishing.md) covers what that takes.\n"
        }
      }
    ],
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md",
        "changelogTitle": "# Changelog\n\nEvery release, newest first. Generated from Conventional Commits — see CONTRIBUTING.md."
      }
    ],
    [
      "@semantic-release/exec",
      {
        "$comment": "`successCmd` hands the version to the workflow. The installers are built after the release exists, from the tag it created, because that commit is the only place tauri.conf.json carries the right version — and an MSI stamped with the wrong one makes Windows treat an upgrade as already-installed.",
        "prepareCmd": "node scripts/release/sync-version.mjs ${nextRelease.version} && npm run build -w @proc123/extension -- --zip",
        "successCmd": "echo \"version=${nextRelease.version}\" >> \"$GITHUB_OUTPUT\""
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": [
          "CHANGELOG.md",
          "README.md",
          "package.json",
          "packages/extension/manifest.chrome.json",
          "packages/extension/manifest.firefox.json",
          "packages/companion/src/http.ts",
          "packages/app/src-tauri/tauri.conf.json"
        ],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "packages/extension/dist/proc123-chrome-*.zip",
            "label": "Extension — Chrome, Edge, Brave"
          },
          {
            "path": "packages/extension/dist/proc123-firefox-*.zip",
            "label": "Extension — Firefox"
          },
          {
            "path": "artifacts/proc123-linux-x64",
            "label": "Companion — Linux (x64)"
          },
          {
            "path": "artifacts/proc123-win32-x64.exe",
            "label": "Companion — Windows (x64)"
          }
        ],
        "successComment": false,
        "failComment": false
      }
    ]
  ]
}
