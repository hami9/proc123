{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    [
      "@semantic-release/release-notes-generator",
      {
        "$comment": "Every release page is someone's first contact with the project, and the commit list alone does not tell them how to install it. `writerOpts` is merged over the preset's, so only the footer changes. `{{version}}` interpolates, which keeps the asset filenames right without anyone editing this per release. The Firefox note is here because about:addons rejecting the zip is the single most common way this install goes wrong.",
        "writerOpts": {
          "footerPartial": "\n---\n\n## Installing\n\n**Chrome · Edge · Brave** — unzip `proc123-chrome-{{version}}.zip`, open `chrome://extensions`, turn on **Developer mode**, then **Load unpacked** and pick the unzipped folder.\n\n**Firefox** — open `about:debugging#/runtime/this-firefox`, click **Load Temporary Add-on…**, and select `proc123-firefox-{{version}}.zip` directly. No need to unzip.\n\n> Do **not** use `about:addons` → Install Add-on From File. That screen only accepts a Mozilla-signed file and rejects the zip as \"not verified\". Firefox has required signatures since version 48, and `xpinstall.signatures.required` is ignored on release builds, so no setting changes it. A temporary add-on lasts until you close the browser.\n\n**A terminal, no browser** — download `proc123-win32-x64.exe` or `proc123-linux-x64`, then `proc123 <category-url> -o products.csv`.\n\nproc123 is not in the Chrome Web Store or listed on addons.mozilla.org yet, which is why the install is a folder rather than a button — [publishing.md](https://github.com/hami9/proc123/blob/main/docs/publishing.md) covers what that takes.\n"
        }
      }
    ],
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md",
        "changelogTitle": "# Changelog\n\nEvery release, newest first. Generated from Conventional Commits — see CONTRIBUTING.md."
      }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "node scripts/release/sync-version.mjs ${nextRelease.version} && npm run build -w @proc123/extension -- --zip"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": [
          "CHANGELOG.md",
          "README.md",
          "package.json",
          "packages/extension/manifest.chrome.json",
          "packages/extension/manifest.firefox.json",
          "packages/companion/src/http.ts"
        ],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "packages/extension/dist/proc123-chrome-*.zip",
            "label": "Extension — Chrome, Edge, Brave"
          },
          {
            "path": "packages/extension/dist/proc123-firefox-*.zip",
            "label": "Extension — Firefox"
          },
          {
            "path": "artifacts/proc123-linux-x64",
            "label": "Companion — Linux (x64)"
          },
          {
            "path": "artifacts/proc123-win32-x64.exe",
            "label": "Companion — Windows (x64)"
          }
        ],
        "successComment": false,
        "failComment": false
      }
    ]
  ]
}
